# docker/clients/

Reserved for Docker assets (Dockerfiles, Compose overlays) belonging
to future client applications under [`clients/`](../../clients/).

Empty today — no client exists yet. See [`docker/server/`](../server/)
for the pattern this directory will follow once one does.
